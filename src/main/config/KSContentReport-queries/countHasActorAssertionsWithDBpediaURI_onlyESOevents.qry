#number of actor assertions to DBpedia actor on eso events
SELECT (COUNT ( * ) as ?count)
WHERE {
    ?e rdf:type sem:Event , ?type . 
    ?type nwr:isClassDefinedBy eso: .
	?e sem:hasActor ?a . FILTER (CONTAINS(STR(?a), "dbpedia.org"))
}